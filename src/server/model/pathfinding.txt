If pursuing:
  Filter characters: in range, line of sight:
  find closest character

    set character positionm as alert point
    go to alert point
  else
   if alert point is defined
     if alert point is visible:
       go to alert point
     else:
       set alert point path
  
   else: wander
Evading
  if it: in range, line of sight: flee
  else: wander